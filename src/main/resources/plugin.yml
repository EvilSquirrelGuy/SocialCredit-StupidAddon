# human-facing metadata
name: ${name}
version: ${version}
prefix: ${prefix}
description: ${description}
website: ${website}

# authorship
authors: ${authors}
contributors: ${contributors} # if you contributed, you can add your github username here

# dev stuff
api-version: ${api_version}
main: ${main}
depend: [SocialCredit]